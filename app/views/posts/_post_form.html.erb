<%= form_for post do |f| %>
  <%= f.label current_user.username %><br/>
  <%= f.text_area :content %>
  <%= f.hidden_field :tea_id, value: "#{@tea.id}"  %>
  <%= f.hidden_field :user_id, value: "#{@user.id}" %><br/>
  <%= f.submit 'Publish' %>
<% end %>
