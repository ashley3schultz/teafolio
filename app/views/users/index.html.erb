<h1>Users</h1>
<% @users.each do |user| %>
    <%= user.username %><%= link_to " - Update Status", edit_user_path(user) %>
    <p>Teas: <%= user.pending_teas.size %> | 
    Posts: <%= user.posts.size %> | 
    Status: <%= user.admin %></p><br/><br/>
<% end %>